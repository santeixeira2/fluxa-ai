bind 0.0.0.0
protected-mode no

# Persist to disk (snapshot every 60s if >= 1 key changed)
save 60 1
dir /data
dbfilename dump.rdb

# Max memory: 256mb, evict least-recently-used keys first
maxmemory 256mb
maxmemory-policy allkeys-lru

# Logging
loglevel notice
